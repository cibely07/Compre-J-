laravel new compreja

cd compreja

php artisan serve

git init
git add .
git commit -m "Projeto Laravel inicial"
git branch -M master
git remote add origin git@github.com:SEUUSUARIO/compreja.git
git push -u origin master

php artisan install:api

php artisan migrate
